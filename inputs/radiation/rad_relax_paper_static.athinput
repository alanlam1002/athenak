<comment>
problem = Discrete-ordinate (DO) equilibration test, matched to arXiv:2302.04283
  Section 3.6 "Equilibration" (static case). Static fluid, Gamma=5/3, rho=1,
  pgas0=2 (=> internal energy density e_gas0=pgas0/(Gamma-1)=3, fed via
  <problem>/temp -- rad_relax.cpp assigns this straight to the IEN primitive,
  so "temp" here literally means e_gas0, not T; see the pgen comment),
  isotropic coordinate-frame radiation field with urad0=1 (arad=1, kB/(mu*mp)=1
  per the paper's unit choice), scattering off (alpha_s=0), absorption
  alpha_a=0.1. Paper predicts (Eq. 68) rho*T_equil/(Gamma-1) + T_equil^4 =
  ugas0 + urad0 = 4, and a coupled ODE trajectory (Eq. 69) en route -- see
  check_rad_relax_paper_equilibration.py. Companion to the moving case,
  rad_relax_paper_moving.athinput, and to the M1-side matched tests
  rad_m1_photon_equilibration_paper_{static,moving}.athinput (Stage 7).

<job>
basename = relax_paper_static  # name of run

<time>
evolution  = dynamic  # dynamic/kinematic/static
integrator = rk2      # time integration algorithm
cfl_number = 0.3       # The Courant, Friedrichs, & Lewy (CFL) Number
nlim       = -1       # cycle limit
tlim       = 20.0     # time limit -- several coupling times (alpha_a)^-1=10

<mesh>
nghost = 2         # Number of ghost cells
nx1    = 4         # Number of zones in X1-direction
x1min  = 0.0       # minimum value of X1
x1max  = 1.0       # maximum value of X1
ix1_bc = periodic  # inner-X1 boundary flag
ox1_bc = periodic  # outer-X1 boundary flag

nx2    = 1         # Number of zones in X2-direction
x2min  = 0.0       # minimum value of X2
x2max  = 1.0       # maximum value of X2
ix2_bc = periodic  # inner-X2 boundary flag
ox2_bc = periodic  # outer-X2 boundary flag

nx3    = 1         # Number of zones in X3-direction
x3min  = -0.5      # minimum value of X3
x3max  = 0.5       # maximum value of X3
ix3_bc = periodic  # inner-X3 boundary flag
ox3_bc = periodic  # outer-X3 boundary flag

<coord>
general_rel = true  # w/ general relativity
minkowski = true    # flat space

<hydro>
eos         = ideal    # EOS type
reconstruct = plm      # spatial reconstruction method
rsolver     = hlle     # Riemann-solver to be used
gamma       = 1.666666666666667  # Gamma=5/3, matches the paper's equilibration test

<radiation>
nlevel = 2     # geodesic-mesh angular resolution (0-D test -- physics is
               # insensitive to this; chosen to match the existing
               # rad_diffusion.athinput default)
arad = 1.0     # radiation constant (paper's unit choice: arad=1)
kappa_s = 0.0  # scattering opacity -- paper's alpha_s = 0
kappa_a = 0.1  # absorption opacity -- paper's alpha_a = 0.1
kappa_p = 0.0  # planck minus rosseland opacity -- unused here

<problem>
erad = 1.0    # initial radiation energy density urad0 = 1 (paper's Eq. 68)
temp = 3.0    # e_gas0 = pgas0/(Gamma-1) = 2/(2/3) = 3 (NOT literal T -- see
              # comment above and in rad_relax.cpp)
v1   = 0.00   # static case
rad_frame = fluid  # irrelevant at v1=0 (fluid and coordinate frames coincide)

<output1>
file_type   = tab            # output format
data_format = %24.16e        # output data format
variable    = rad_hydro_w    # dens,velx,vely,velz,eint + r00..r33 + r00_ff..r33_ff
dt          = 0.05           # output cadence
